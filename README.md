# ImageLibrarr
A modern full-stack image library system powered by Next.js and Node.js. Image metadata and operations are handled with MongoDB, while PostgreSQL is used for authentication and relational user data. Supports image upload, search, tagging, and access control.
